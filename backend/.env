NODE_ENV=development
PORT=5000
# Set USE_MOCK_DB=true to use in-memory database (no MongoDB installation needed)
# Set USE_MOCK_DB=false and add MongoDB Atlas URI to use real database
USE_MOCK_DB=true
MONGODB_URI=mongodb://localhost:27017/etcp
JWT_SECRET=your-secret-key-here
JWT_EXPIRES_IN=7d